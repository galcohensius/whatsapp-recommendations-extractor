# Python version requirement: >= 3.8
# (for typing support and pathlib features)

# External dependencies:
openai>=1.0.0

# Backend API dependencies (if running backend)
# For backend, install from backend/requirements.txt instead:
# pip install -r backend/requirements.txt

